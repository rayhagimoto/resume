\BLOCK{import 'partials/macros.tex' as macros}

\BLOCK{call macros.BaseSection('EXPERIENCE')}
  \BLOCK{for item in experience}
    \VAR{macros.BaseEntry(
      item.organization,
      item.dates,
      item.title,
      item.location,
      macros.AddBullets(item.bullets) if item.bullets else ""
    )}
  \BLOCK{endfor}
\BLOCK{endcall}
