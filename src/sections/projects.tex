\BLOCK{import 'partials/macros.tex' as macros}

\BLOCK{call macros.BaseSection('PROJECTS')}
  \BLOCK{for item in projects}
    \VAR{macros.BaseEntry(
      item.title,
      item.dates,
      item.organization,
      item.location,
      (macros.AddBullets(item.bullets) if item.bullets else "")
      + (macros.AddDescription(item.description) if item.description else "")
    )}
  \BLOCK{endfor}
\BLOCK{endcall}
